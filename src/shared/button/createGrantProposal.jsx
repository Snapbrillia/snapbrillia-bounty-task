import '../css/buttonStyles.css';

const CreateGrantProposal = (props) => {
  return (
    <button {...props} className="btn-primary">
      Create Grant Proposal
    </button>
  );
};

export default CreateGrantProposal;
